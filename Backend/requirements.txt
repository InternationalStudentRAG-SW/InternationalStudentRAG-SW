# --- 웹 프레임워크 및 서버 ---
fastapi[standard]
uvicorn

# --- LangChain 및 RAG 핵심 프레임워크 (의존성 충돌 방지 최신 버전) ---
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
openai>=1.3.0

# --- 데이터 추출 및 전처리 (구조적 파싱 필수 패키지) ---
# 표 구조 보존 및 텍스트 추출을 위해 추가됨
pdfplumber>=0.10.0
beautifulsoup4>=4.12.0
requests>=2.31.0
python-dotenv>=1.0.0
pydantic-settings>=2.1.0

# --- 논문 기반 Hybrid Retrieval Pipeline 필수 패키지 ---
# 영어 줄임말 해제, 언어 감지, 형태소 분석을 위해 추가됨
nltk==3.9.4
contractions==0.1.73
konlpy==0.6.0
langdetect>=1.0.9

# --- 하이브리드 검색: BM25 알고리즘 구현 ---
# 키워드 기반 검색(Lexical Search)을 위해 추가됨
rank_bm25==0.2.2

# --- 데이터베이스 및 ORM (PostgreSQL) ---
psycopg2-binary>=2.9.9
psycopg[binary]>=3.1.18
sqlalchemy>=2.0.0

# --- 평가 및 데이터 분석 ---
# RAG 성능 측정 및 결과 저장을 위해 추가됨
ragas>=0.2.0
datasets>=2.18.0
pandas>=2.0.0