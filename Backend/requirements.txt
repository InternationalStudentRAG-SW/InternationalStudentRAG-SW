# ── 웹 프레임워크 ─────────────────────────────────────────
fastapi==0.135.2
uvicorn==0.42.0
python-multipart==0.0.22      # 파일 업로드
httpx==0.28.1

# ── 설정 / 인증 ───────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2
PyJWT==2.13.0

# ── LangChain ─────────────────────────────────────────────
langchain==1.2.13
langchain-core==1.2.23
langchain-openai==1.1.12
langchain-chroma==1.1.0
langchain-community==0.4.1
langchain-classic==1.0.3
langchain-text-splitters==1.1.1

# ── OpenAI ────────────────────────────────────────────────
openai==2.30.0

# ── 벡터 DB ───────────────────────────────────────────────
chromadb==1.5.5

# ── 하이브리드 검색 ───────────────────────────────────────
rank-bm25==0.2.2              # BM25Retriever 의존성

# ── 리랭킹 모델 (BGE Reranker) ────────────────────────────
sentence-transformers==5.3.0
torch==2.11.0
transformers==5.5.0
accelerate==1.13.0

# ── PDF 처리 ──────────────────────────────────────────────
PyMuPDF==1.27.2.3
pymupdf4llm==1.27.2.3

# ── 다국어 처리 ───────────────────────────────────────────
langdetect==1.0.9
kss==6.0.6                    # 한국어 문장 분리
pysbd==0.3.4                  # 다국어 문장 분리
numpy==2.4.4

# ── 지식그래프 (Neo4j) ────────────────────────────────────
neo4j==6.3.1

# ── DB / 인증 (Supabase) ──────────────────────────────────
supabase==2.30.0
SQLAlchemy==2.0.48

# ── 캐시 (Upstash Redis) ──────────────────────────────────
redis==8.1.0

# ── RunPod Serverless ─────────────────────────────────────
runpod==1.7.9
requests==2.32.5
